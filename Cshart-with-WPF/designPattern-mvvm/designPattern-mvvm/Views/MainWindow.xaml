<Window x:Class="designPattern_mvvm.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:designPattern_mvvm"
        mc:Ignorable="d"
        xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=Scottplot.WPF"
        Title="시험점수그래프" Height="450" Width="800">
    
    <!--
        View의 역할: 화면 배치, Binding만 담당. logic X
    -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Content="점수 생성" Height="40" Margin="10" Command="{Binding GenerateCommand}" />
        <scottplot:WpfPlot x:Name="PlotView" 
                           Grid.Row="1"
                           Margin="10" />
    </Grid>
</Window>
